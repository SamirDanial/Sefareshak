/**
 * tse.csv
 *
 * TODO: Not yet implemented.
 * tse.csv contains TSS (Technische Sicherheitseinrichtung) device data.
 * TSS information is currently embedded per-transaction in transactions_tse.csv
 * via the security field. Implement a dedicated tse.csv builder when TSS device
 * metadata reporting at the closing level is required.
 */
export function buildTse(): any[] {
  return [];
}
